// Placeholder de entrypoint para o esqueleto compilar. A composição real do host
// (health, observabilidade, wiring do consumer e modo Job) entra em outros commits.
WebApplication.CreateBuilder(args).Build().Run();
